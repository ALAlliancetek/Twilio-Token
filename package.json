{
  "name": "twilio-token-server",
  "version": "1.0.0",
  "description": "Twilio Access Token server for flutter_twilio_commkit — deploy to Render.com",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "twilio": "^5.3.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  }
}
